source "https://rubygems.org"

# GitHub Pages pins the Jekyll version and plugin set used by the live site,
# so building against this gem keeps local previews faithful to production.
gem "github-pages", group: :jekyll_plugins

# No longer a default gem as of Ruby 3.0; `jekyll serve` needs it.
gem "webrick"
